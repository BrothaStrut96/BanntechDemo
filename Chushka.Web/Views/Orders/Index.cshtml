@model OrderViewModel

@{
    ViewData["Title"] = "Home Page";
}

<h1 class="text-center">All Orders</h1>
<hr class="hr-2 bg-dark" />
<table class="table w-75 mx-auto table-hover">
    <thead>
        <tr class="row">
            <th class="col-md-1">#</th>
            <th class="col-md-4">Order ID</th>
            <th class="col-md-2">Customer</th>
            <th class="col-md-2">Product</th>
            <th class="col-md-2">Ordered On</th>
        </tr>
    </thead>
    <tbody>
        @{ var tablerow = 0;}
        @foreach (var order in Model.Orders)
        {
            <tr class="row">
                <th class="col-md-1">@tablerow</th>
                <td class="col-md-4">@order.Id</td>
                <td class="col-md-2">@order.Customer</td>
                <td class="col-md-2">@order.Product.Name</td>
                <td class="col-md-2">@order.OrderCreated</td>
            </tr>
            tablerow += 1;
        }
    </tbody>
</table>